# requirements-dev.txt — development and test dependencies
#
# Install alongside requirements.txt for local dev and CI:
#   pip install -r requirements.txt -r requirements-dev.txt
#
# These dependencies are NOT needed in production, only for local development,
# testing, and CI. They enable coverage measurement, debugging, S3 mocking, and test discovery.

pytest>=8.0
pytest-cov>=5.0.0
pytest-flask>=1.3
fakeredis>=2.20
moto[s3]>=5.0
locust==2.30.0
