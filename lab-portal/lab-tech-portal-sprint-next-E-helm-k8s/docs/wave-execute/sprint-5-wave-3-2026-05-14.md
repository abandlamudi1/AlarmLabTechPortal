# Sprint 5 Wave 3 Summary — 2026-05-14

> Generated by `/wave-execute` Phase 5. Becomes the canonical record for this wave. The "Human Verdict" section at the bottom is the human-in-the-loop checkpoint — leave it blank during generation; fill it in during review.

## Outcomes

| Stream | PR | Branch | Issues | Copilot rounds | CI re-fixes | Status |
|--------|-----|--------|--------|----------------|-------------|--------|
| J | #102 | feat/sprint-5-J-async-ui | #18 | 1 | 0 | ready to merge |

One stream produced a PR and reached CI-green within one Copilot review round. Zero CI re-fixes needed.

## Copilot review comment breakdown

| Stream | Total | Fixed | Scope | Deferred | Dismissed |
|--------|-------|-------|-------|----------|-----------|
| J (#102) | 2 | 2 | 0 | 0 | 0 |
| **Total** | **2** | **2** | **0** | **0** | **0** |

Both comments were genuine defects: one BUG (task_id not captured at retry dispatch) and one SECURITY (XSS via innerHTML in polling JS). Both fixed in commit `a5eb89a`.

## Friction encountered

### Friction 7 — Wave under-load: single stream executed when compatible issues were available (new)

- **Pattern**: Wave 3 ran with only 1 stream (#18) despite 4+ open "Next" milestone issues (#89, #90, #99, #100) that had no file overlap with Stream J and no blocking dependencies. Sprint velocity for this wave was ~3 story points instead of a potential ~15.
- **Root cause**: The sprint plan was fixed at Wave 1 kickoff time. Issues #99 and #100 were created mid-sprint (as SCOPE follow-ups from Wave 2 Copilot review) and were never added to the plan. Issues #89 and #90 existed but were excluded from the sprint scope at planning time. `/wave-execute` had no mechanism to detect or supplement an under-loaded wave.
- **Recurrence**: `new` — first observation
- **Mitigation applied this wave**: None in-wave. Identified at Wave 3 kickoff; structural fix applied to skills.
- **Proposed action**: ✅ **Done** — added Step 0.3.5 (Under-load check) to `wave-execute` SKILL.md and Phase 1.1.5 (Sprint Capacity Check) to `plan-parallel-streams` SKILL.md. Issues #104 (plan-parallel-streams) and #105 (wave-execute) created for tracking.

## Patterns observed

- **Review classification distribution**: 2 Copilot comments on 1 stream. 1 BUG (retry_jira_sync discarding AsyncResult.id), 1 SECURITY (innerHTML XSS sink in async polling JS). No FALSE_POSITIVE, no STYLE, no SCOPE.
- **XSS in async polling JS**: Both Celery-polling scripts (print_request_detail.html and jira_import.html) used innerHTML for dynamic error messages. This is a repeatable pattern — any future async polling UI that inserts user-visible data must use textContent/DOM methods, not innerHTML. Agents should be briefed on this for Wave 3+ streams.
- **Wave under-load is the dominant wave-3 story**: The single-stream wave is a process gap, not a code quality gap. With the skill fixes applied, Sprint 6 should not have this problem.
- **14 new tests in test_async_ui.py**: Stream J added strong coverage for the new async UI paths. The test suite reached 257 passed.

## Human Verdict redirect: sprint sizing

The Wave 2 human verdict raised the concern that sprints are too small. Analysis based on Wave 1–3 data:

### Current velocity
- Sprint 5: 12 implementation issues across 3 waves (4+5+1 streams). Estimated ~18–22 story points.
- Max demonstrated parallelism: 5 concurrent streams (Wave 2).
- Wave 3 under-load: 1 stream where 5 could have run.

### Root cause of under-sizing
The planning skill was scoping to a named slice only, not to the full "Next" milestone backlog. Sprint 5 had ~20 "Next" issues available at planning time but only 12 were scoped. At wave-execute time, there was no catch-up mechanism.

### Recommendation for Sprint 6
**Target: 30–35 story points** across 2 waves with 7–8 streams per wave:
- Wave 1: Slice G-part1 Docker foundations (~6 issues) + skill infra fixes #77–84 (~4 issues) = 10 streams
- Wave 2: Sprint 5 follow-ups #89, #90, #99, #100 (~4 issues) + any newly ready Slice G-part1 deps = 5–6 streams
- Skill change applied: `plan-parallel-streams` and `wave-execute` now enforce a 20–30 SP target and auto-supplement small waves

### Skill modifications applied this wave
Both changes are live in the skill files:
- **plan-parallel-streams Phase 1.1.5**: capacity check + backlog augmentation if < 8 issues scoped
- **wave-execute Step 0.3.5**: under-load detection + auto-add compatible issues in `--auto` mode

## Human Verdict redirect: UI 2026 standards

The Wave 2 human verdict noted the UI is outdated and asked about architecture guide recommendations.

### Findings
The `qe-architecture-ai-assisted-guide` has **no CSS design system, component library, or accessibility pattern**. The only frontend production pattern covers React Query + WebSocket state — not applicable to the portal's Flask/Jinja2 template stack.

### Gaps identified
- No design tokens / CSS custom properties pattern
- No WCAG 2.1 AA baseline documentation
- No responsive design guidance
- No pattern for dark mode in SSR templates (portal has a dark mode toggle but no guide pattern to cite)

### Action taken
Issue **#103** created in lab-tech-portal: "docs(guide-friction): architecture guide has no UI design system or frontend standards pattern". Scoped as Slice H closure work (Sprint 6 or 7).

The portal's `style.css` works but has never been audited against WCAG 2.1 AA. The #103 issue includes the audit as part of its scope.

## Recommended next actions

Proposed for Sprint 6 groom. Tick boxes during human review; ticked items become GitHub issues.

- [x] **Add wave under-load detection to wave-execute** — tracked as #105. Done: Step 0.3.5 added to SKILL.md.
- [x] **Add sprint capacity maximization to plan-parallel-streams** — tracked as #104. Done: Phase 1.1.5 added to SKILL.md.
- [x] **UI design system guide gap** — tracked as #103. Issue open; scope includes guide pattern write-up + portal a11y audit.
- [ ] **XSS-via-innerHTML rule for polling JS** — add to AGENTS.md hard rules: "Never use innerHTML to render server-sourced data. Use textContent or DOM element construction." Recurrence risk is high — every future async polling template has this footgun. Milestone: Sprint 6, label: `documentation`.
- [ ] **Sprint 6 scope: target 30–35 story points** — use Plan 1.1.5 at Sprint 6 planning to pull full "Next" milestone rather than slice-only scope. No issue needed — process change.
- [ ] **Skill infra issues #77–84** — these are all "Next" milestone and should be in Sprint 6 Wave 1 if file footprints allow. Review at Sprint 6 plan-parallel-streams run.

## Data-points entry (auto-appended)

```
| 2026-05-14 | Sprint 5 | Wave 3 | 1 (J) | Sonnet×1 | 1 | 0 | #102 | new:F07(wave-under-load), new:XSS-in-polling-JS |
```

---

## Human Verdict

> Fill in after reviewing this summary. The next `/wave-execute` reads this section during pre-flight.

**On track?**: yes 

**Redirect needed?**: no 

**Approved proposed issues**: all recommended next actions approved

**Carry-forward observations**: to me a correctly identifyied friction point is being addressed with 104 and 105. Additionally, i wonder if we can use some of the universal style guide shown here: /Users/snodder/dev/work/FW-evaluation-process/docs/design-system/README.md

**Reviewed by**: Stephen Nodder
**Reviewed on**: 2026-05-14
