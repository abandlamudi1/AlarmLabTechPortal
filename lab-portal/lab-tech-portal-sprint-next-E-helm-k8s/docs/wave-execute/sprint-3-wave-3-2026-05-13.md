# Sprint 3 Wave 3 Summary — 2026-05-13

> Closes out Sprint 3 (Slice B observability + Slice D-part1 data layer + infra housekeeping). Three waves in total — this is the final one.

## Outcomes

| Stream | PR | Branch | Copilot rounds | CI re-fixes | Time-to-merge | Status |
|--------|-----|--------|----------------|-------------|---------------|--------|
| A — Slice B completion | #69 | `feature/sprint-3-slice-b-completion` | 1 (2 comments, both FIX) | 0 | ~6h | merged |
| B — Infra housekeeping | #67 | `feature/sprint-3-infra-housekeeping` | 1 (6 comments, all FIX) | 2 | ~8h | merged |

## Friction encountered

### Item 1: Pre-existing regression from PR #62 (Slice D-part1)
- **Pattern**: `app.py`'s `readyz` endpoint referenced `_inventory_module.DB` and `_rf_module.DB_FILE` after Slice D-part1 moved DB access to per-tool `db.py` files
- **Root cause**: PR #62 didn't grep for callers of the moved attributes; reviewer (Copilot) didn't either
- **Recurrence**: [[F01]] — first observation
- **Mitigation applied this wave**: fixed in PR #67 (commits `0ad0ebd`, `de488bc`); added AGENTS.md hard rule + pr-review-tiered.md never-soften rule (PR #70)
- **Proposed action**: none — rule + review change is sufficient

### Item 2: CI poll premature exit
- **Pattern**: wave-execute SKILL.md used `.conclusion // .status` which short-circuits on empty-string conclusion
- **Root cause**: GitHub returns `""` not `null` for in-progress check conclusions
- **Recurrence**: [[F02]] — first observation
- **Mitigation applied this wave**: skill updated in PR #67 commits to use `.status` directly with `select(.name != null)`
- **Proposed action**: none — already fixed

### Item 3: Stream A kept dead `_inventory_module` and `_rf_module` imports
- **Pattern**: Dev agent added new import but left old one in place; surfaced as merge conflict after Stream B's fixes landed in master
- **Root cause**: agent prompt template doesn't instruct to grep-and-remove stale aliases when adding replacement imports
- **Recurrence**: [[F03]] — first observation
- **Mitigation applied this wave**: AGENTS.md hard rule (PR #70); manually resolved during rebase
- **Proposed action**: update `sprint-kickoff`'s Dev agent prompt template — see "Recommended next actions" below

## Patterns observed

- **Review classification distribution**: 8/8 Copilot comments classified as FIX (mix of ROBUSTNESS and STYLE). No SCOPE items, no DUPLICATEs, no FALSE_POSITIVEs. The tiered-review skill handled both PRs cleanly.
- **Common failure modes**: Both streams failed CI on the same pre-existing regression (F01) — same root cause, different surface symptoms. Cost ~3h of wall-clock fixing it twice in different worktrees.
- **Time-to-merge insights**: Stream B took ~2× Stream A's time entirely due to inherited regression. Without F01, both would have closed in ~4h.
- **Wave-execute skill maturity**: third data point in, the orchestration loop is stable. The remaining friction is upstream (agent prompts, PR review depth) not in the orchestration mechanism itself.

## Recommended next actions

Proposed for Sprint 4 groom. Tick boxes during human review.

- [ ] **Update sprint-kickoff Dev agent prompt template to instruct grep-and-remove of stale module imports** — milestone: Sprint 4, labels: `wave-friction`, `tooling`. Rationale: addresses F03 root cause directly; without this, the agent will keep adding new imports alongside old ones.
- [ ] **Add a pre-commit `python -c "import app"` sanity check in the worktree validation step** — milestone: Sprint 4, labels: `wave-friction`, `tooling`. Rationale: would have caught F01 in seconds locally instead of after a CI round-trip.
- [ ] **Document the changelog vs wave-summary boundary in AGENTS.md** — milestone: Sprint 4, labels: `docs`. Rationale: clarifies which artifact to write when working in a wave vs. solo.

## Data-points entry (auto-appended)

```
| 2026-05-13 | 3 | 3 | 2 | Sonnet × 2 | 1 | 2 (PR #67) | 2 (#67, #69) | [[F01]], [[F02]], [[F03]] |
```

---

## Human Verdict

> Fill in after reviewing this summary. The next `/wave-execute` reads this section during pre-flight.

**On track?**: yes 

**Redirect needed?**: same comments as wave 2, perhaps we can be more ambitious with our sprints

**Approved proposed issues**:

**Carry-forward observations**:

**Reviewed by**: stephen nodder
**Reviewed on**: 2026-05-14
